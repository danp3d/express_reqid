{
  "name": "express_reqid",
  "version": "1.0.2",
  "description": "Express middleware to include a unique request ID (for request tracing)",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive --reporter spec && npm run coverage",
    "coverage": "istanbul cover _mocha -- --recursive -R spec",
    "coverage-win": "istanbul cover node_modules/mocha/bin/_mocha -- --recursive -R spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danp3d/express_reqid.git"
  },
  "author": {
    "email": "daniel.exe@gmail.com",
    "name": "Daniel Pedroso"
  },
  "license": "ISC",
  "dependencies": {
    "uuid": "^3.0.0"
  },
  "devDependencies": {
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5",
    "should": "^8.3.2"
  }
}
